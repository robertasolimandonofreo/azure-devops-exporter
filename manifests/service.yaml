apiVersion: v1
kind: Service
metadata:
  name: azure-devops-exporter
  labels:
    app: azure-devops-exporter
spec:
  selector:
    app: azure-devops-exporter
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
